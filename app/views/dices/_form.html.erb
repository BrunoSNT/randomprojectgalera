<%= form_with(model: dice, local: true) do |form| %>
  <% if dice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dice.errors.count, "error") %> prohibited this dice from being saved:</h2>

      <ul>
      <% dice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :sides %>
    <%= form.number_field :sides %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
